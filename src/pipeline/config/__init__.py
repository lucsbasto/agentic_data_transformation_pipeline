"""Typed runtime configuration for the agentic pipeline (F7).

Re-exports :class:`Settings` so callers can ``from pipeline.config import Settings``.
"""

from pipeline.config.settings import Settings

__all__ = ["Settings"]
