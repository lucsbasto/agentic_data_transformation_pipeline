# F1.6 Code Review

**Verdict: APPROVE**

| Severity | Count |
|----------|-------|
| CRITICAL | 0 |
| HIGH     | 0 |
| MEDIUM   | 2 |
| LOW      | 1 |

## Top 3 Findings

### [MEDIUM] `LLMResponse` docstring contradicts code
`src/pipeline/llm/client.py:33-35` — Docstring says "F1.7 extends this with per-call cache-hit flag" but `cache_hit: bool` is already present at line 40. Either remove `cache_hit` from the F1 stub (defer to F1.7) or fix the docstring to list only the fields that are actually deferred (retry count, cost estimate, post-fallback model).
**Action:** Update docstring to reflect that `cache_hit` ships in F1; list only `retry_count`, `cost_usd`, `resolved_model` as F1.7 additions.

### [MEDIUM] `_run_ingest` is 87 lines
`src/pipeline/cli/ingest.py:85-171` — Exceeds the 30-line guideline (skill) and 50-line checklist target. The manifest-open / try-transform / mark-completed phases are distinct and could be extracted (e.g., `_open_batch`, `_execute_transform`, `_close_batch`).
**Action:** Consider splitting in a future refactor pass; acceptable for F1 given the linear orchestration nature.

### [LOW] `standalone_mode=True` is redundant
`src/pipeline/__main__.py:21` — `standalone_mode=True` is Click's default. Passing it explicitly adds no value.
**Action:** Optional cleanup; remove the kwarg for brevity.

## Well-Done

- **Error/context lifecycle is correct.** `clear_context()` in `finally` (ingest.py:79) ensures structlog context is cleaned on both success and failure paths. `PipelineError` is caught and translated to `ClickException` so the CLI always exits cleanly.
- **Test isolation is thorough.** Integration tests wipe all relevant env vars, use absolute `PIPELINE_STATE_DB` path under `tmp_path`, and `monkeypatch.chdir` — no leakage into real project state. The `tiny_source_parquet` fixture keeps tests self-contained.
