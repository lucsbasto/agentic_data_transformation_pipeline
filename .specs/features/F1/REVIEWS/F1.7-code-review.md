# F1.7 Code Review — LLMCache + LLMClient

**Verdict: COMMENT** (no blocking issues for F1.7 merge)

| Severity | Count |
|----------|-------|
| CRITICAL | 0 |
| HIGH     | 1 |
| MEDIUM   | 2 |
| LOW      | 1 |

## Top 3 Findings

1. **[HIGH] `cache.py:49-58` — cache key `-0.0` divergence + spec field mismatch.**
   `temperature=-0.0` formats as `"-0.000000"` vs `"0.000000"` for `0.0`, producing different keys for equal floats. Spec also requires `schema_name` in the key but impl uses `max_tokens` instead.
   → Normalize with `temperature = temperature + 0.0` before formatting. Reconcile key fields with spec before F1.8.

2. **[MEDIUM] `client.py:62` — `_AnthropicLike.messages: Any` defeats static checking.**
   → Add a nested `_MessagesLike(Protocol)` with a typed `create()` signature.

3. **[MEDIUM] `cache.py:107-120` — `INSERT OR IGNORE` silently drops token-count corrections.**
   → Intentional for determinism; add a clarifying comment for future maintainers.

## Well Done

- Retry loop sleep guard (`if attempt < budget`) is correct — no wasted sleep, clean fallback separation, proper `from last_exc or exc` chaining.
- `sleeper` DI for backoff control in tests is elegant and avoids `mock.patch` fragility.
